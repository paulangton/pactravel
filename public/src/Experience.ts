// An experience consists of the entry method to a city, the exit method, and
// the visits pacman makes throughout the city
class Experience {
  city : String;
  entry : Hop;
  exit : Hop;
  visits : ANode[];

  // Renders an experience
  draw() {
    
  }
}
